---
driver:
  name: docker 

provisioner:
  name: chef_solo
platforms:
  - name: ubuntu-12.04
    run_list:
    - "recipe[apt]"
    - "recipe[java]" 
    - "recipe[apache2]"
  - name: centos-6.4
    run_list:
    - "recipe[yum]"
    - "recipe[java]"
    - "recipe[apache2]"

suites:
  - name: default
    attributes:
      java:
        install_flavor: openjdk 
        jdk_version: 7

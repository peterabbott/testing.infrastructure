## Customize the test machine
machine:
  services:
    - docker

  # Version of ruby to use
  ruby:
    version:
     2.1.4

dependencies:
  bundler:
    without: [production,  kitchen_vagrant, kitchen_cloud, development  ] 

test:
  override:
    - KITCHEN_DRIVER=docker bundle exec kitchen verify default-ubuntu-1204
